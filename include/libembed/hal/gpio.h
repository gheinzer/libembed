/**
 * @file gpio.h
 * @author Gabriel Heinzer
 * @brief Hardware abstraction layer for general purpose inputs and outputs (GPIOs).
 */

#include <libembed/arch/arch.h>

namespace embed::gpio {
    using namespace embed::arch::gpio;
}